# Enable DEBUG logging for JWT filter
logging.level.com.ead.gearup.filter=DEBUG
logging.level.com.ead.gearup.service.auth=DEBUG

# Database Configuration for Development - Neon PostgreSQL
spring.datasource.url=jdbc:postgresql://ep-late-dawn-a94laone-pooler.gwc.azure.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_LmQs7JdKV9yg
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration for Development
jwt.secret=MyDevelopmentSecretKeyThatIsAtLeast32CharactersLongForHS256Algorithm
# 12 hours in milliseconds
jwt.expiration=43200000                      
# 7 days in milliseconds
jwt.refresh.expiration=604800000
# 5 minutes in milliseconds
jwt.email_verification.expiration=300000

# Email settings for development
# ENABLED for development - emails will be sent
app.email.verification.enabled=true

# Email credentials from environment variables
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
