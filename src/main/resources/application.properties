spring.application.name=gearup-backend
spring.profiles.active=dev

# Base URLs
app.base-url=http://localhost:8080
app.frontend-url=http://localhost:3000

# CORS Configuration
# Comma-separated list of allowed origins (supports wildcards for ports)
# Development: Use localhost with any port
# Production: Use specific domain(s)
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Database Configuration (Environment Variables Required)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.packages-to-scan=com.ead.gearup.controller
springdoc.show-actuator=false

# Custom Swagger URLs
app.swagger.dev-url=http://localhost:8080
app.swagger.prod-url=https://api.gearup.com

# JWT configuration
jwt.secret=${JWT_SECRET}
# 12 hours in milliseconds
jwt.expiration=43200000                      
# 7 days in milliseconds
jwt.refresh.expiration=604800000
# 5 minutes in milliseconds
jwt.email_verification.expiration=300000

# Email settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# Chatbot Service Configuration
chatbot.service.url=${CHATBOT_SERVICE_URL:http://localhost:8000}

# GraphQL
spring.graphql.path=/graphql
spring.graphql.graphiql.enabled=true
spring.graphql.schema.locations=classpath:graphql/

# Admin initialization settings
# Default admin account created on first startup
app.admin.email=admin@gearup.com
app.admin.password=Admin@123
app.admin.name=System Administrator
app.admin.init.enabled=true

# ============================================
# AI Chatbot Configuration
# ============================================

# Python FastAPI Chatbot Service
chatbot.python.service.url=${CHATBOT_PYTHON_SERVICE_URL:http://localhost:8000}
chatbot.python.service.timeout=30

# Redis Configuration for Chat Caching
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=60000
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# Cache Configuration
spring.cache.type=redis
spring.cache.redis.time-to-live=3600000
